name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  test:
    uses: stylelint/.github/.github/workflows/test.yml@reusable-workflow # TODO: Change to `@main`
    with:
      node-version: '["16"]'
